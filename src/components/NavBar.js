import React from 'react';
import {Link} from 'wouter';

class NavBar extends React.Component{
    render() {
        return (
            <h1>Esto es el banner</h1>
        );
      }
    
}

export default NavBar;